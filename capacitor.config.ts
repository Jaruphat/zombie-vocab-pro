import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaruphat.zombievocabpro',
  appName: 'Zombie Vocab Pro',
  webDir: 'dist'
};

export default config;
